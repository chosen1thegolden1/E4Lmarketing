# --- GoHighLevel (Voice AI knowledge base + push) ---
# Private Integration Token: Sub-account > Settings > Private Integrations
GHL_API_KEY=
GHL_LOCATION_ID=zSBqmFrgOtGwd4ALyIsD
# NOTE: this environment already provides GHL_API_TOKEN; the config layer will
# read GHL_API_KEY first, then fall back to GHL_API_TOKEN so it "just works" here.

# --- LLM generator (Claude / Anthropic) ---
ANTHROPIC_API_KEY=
