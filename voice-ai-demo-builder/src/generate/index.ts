// TODO (step 3): one Claude call with a strict JSON schema.
// Consumes RawSite from ../scrape and emits a DemoSpec:
//   { businessName, services[], persona, faqs[], demoCopy{} }
// for the renderer + the GHL Voice AI knowledge base.
export {};
